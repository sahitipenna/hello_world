# SQLite by default (zero setup). For a real multi-instance deployment,
# point this at a hosted Postgres/MySQL instance instead — Prisma's schema
# provider would need to change from "sqlite" to match.
DATABASE_URL="file:./dev.db"
